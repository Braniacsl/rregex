- change String to &str especially for regex string input since well probably want to pre-compile nfa and dfa at some point in application usage.

- Structure application

- Add comprehensive error checking